{
	"nodes":[
		{"id":"0bf2b5ab0042e0ca","x":-204,"y":-371,"width":984,"height":71,"color":"3","type":"text","text":"# What is Routing & Routing Table? | Manage Routing Table in Linux"},
		{"id":"b037b6be57a0fd98","x":-360,"y":-280,"width":780,"height":480,"type":"text","text":"# Routing\nRouting is process which is performed by layer 3 (or network layer) devices in order to deliver the packets by choosing an optimal path from one network to another\n\n### Type of Routing\n1. Static routing\n2. Dynamic routing\n3. Default routing\n\n"},
		{"id":"ad0105954a085beb","type":"text","text":"```\n1. ip route  \n2. ip r\n👉👉 both commands will print the routing table but it is not very readable\n```\n\n```\n1. netstat -rn\n👉👉 You need neststat package to run this command. This will print the route-table in proper format\n\n1. route -n\nThis will also print the route-table in proper format\n```\n\n# Adding New Route in routing table\n\n```\n1. ip route add 10.0.2.0/24 via 192.168.43.223 dev ens160\n\nLet's break it\n👉 10.0.2.0/24 --> is the Destination meaning Route IP\n👉 192.168.43.223 --> is the Gateway\n👉 dev ens160 --> is the interface device name. Make sure ens160 is enabled connection\n\nIf you run \n1. route -n\n```\n\n![[Pasted image 20250208162012.png]]\n\n# Deleting Route from route table\n\n```\n1. ip route del 10.0.2.0/24 via 192.168.43.223 dev ens160\n```\n\n#### NOTE --> All the entry that we did in routing table will remove after reloading the machine to permanent save these routing in routing table. Go 👉👉","x":440,"y":-280,"width":840,"height":1060},
		{"id":"66fcf7b2e2d6c420","x":1300,"y":-280,"width":780,"height":1020,"color":"3","type":"text","text":"# Permanenting Route entries in Routing table\n\n```\n1. cd /etc/sysconfig/network-scripts/\n2. vim route-ens160\n\nNote --> we are creating the file name route-160. Here you can create any name but make it state with 👉 route- and anything\n\n3. 10.0.2.0/32 via 192.168.43.1 \n👉👉👉 This is the entry we need to do inside route-ens160\n\n4. nmcli connection reload\n5. systemctl restart NetworkManager\n```\n\n### For Debian machine \n\n```\n1. cd /etc/network/interfaces\n\n👉👉 This is the path where you will be creating file and do the entry\n```\n\n# Important --> \nWhere these information comes from meaning the information which we see in routing table where does it come from\n\n```\n1. cat /proc/net/route\n👉👉 These file holds the information about IPV4\n\n2. cat /proc/net/ipv6_route\n👉👉 THese file holds the information about IPV6\n\n3. cat /proc/net/rt_cache\n👉👉 These file holds the cache related information\n```"}
	],
	"edges":[]
}