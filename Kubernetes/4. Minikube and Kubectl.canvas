{
	"nodes":[
		{"id":"00c65745c3d2a0e1","type":"text","text":"# Minikube and Kubectl","x":-220,"y":-340,"width":360,"height":60,"color":"3"},
		{"id":"a91ad4010c4e846b","type":"text","text":"# Minikube\n* It is open source tool used to simulate one node cluster locally where master processes and worker processes both run on same node\n* ***\n\n* To use MiniKube you have virtual box in your machine because it run cluster through virtual box or some other hypervisor and cluster node runs in that virtual box\n\n\n# Installation for mac\nBecause MiniKube need virtualization tool so let's install hypervisor first \n\n1. `brew install hyperkit`\n\t1. `hyperkit` is hypervisor\n2. `brew install minikube` \n\t1. because it has `kubectl` dependency it will download `kubectl` as well\n3. run `kubectl`\n\t1. if it gives you the output it means it has been installed successfully\n4. also run `minikube`\n\t1. if it gives you the output it means it has been installed successfully\n\n\n# MiniKube commands\n1. `minikube start --vm-driver=hyperkit`\n\t1. It will start the cluster locally using the `hyperkit hypervisor` for virtualization\n2. `minikube status`\n\t1. Give status of cluster\n3. `minikube delete`\n\t1. it will delete the cluster\n4. `minikube start --vm-driver=hyperkit --v=7 --alsologtostderr`\n\t1. it will start the cluster in debug mode\n\n# Here,\n* Minikube is for start up/deleting the cluster\n\n\n\n\n","x":-760,"y":-260,"width":700,"height":1060,"color":"5"},
		{"id":"1da7684d07dd1d51","type":"text","text":"# Kubectl\n\n* It is command line tool used to interact with cluster to make Kubernetes components like `Pods, servers, insgress and so on`\n\n***\n\n# Kubectl command\n\n1. `kubectl get nodes`\n\t1. It will give the node ( cluster ) status\n2. `kubectl version`\n\t1. give you the `clinet version` and `server version`\n\n### Here, \n * Kubectl is for configuring the Minikube cluster","x":-40,"y":-260,"width":700,"height":680,"color":"4"}
	],
	"edges":[]
}